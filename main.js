//user can select an empty box
//user can win if 3 box's in a row are selected
//users can tie if all box's are full

//add play x/o class
//remove unselected class

let emptyBox  = document.getElementsByClassName('unselected-box')
let xSelected = document.getElementsByClassName('selected-by-x')
let oSelected = document.getElementsByClassName('selected-by-o')


if(xSelected || oSelected){

}

//check if there's a winner
//check box's status so as not to override
